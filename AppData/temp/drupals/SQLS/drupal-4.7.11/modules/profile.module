SELECT name, title, weight, visibility FROM {profile_fields} WHERE visibility IN (%d, %d) ORDER BY weight;
SELECT name, title, type, visibility, weight FROM {profile_fields} WHERE visibility IN (%d, %d) ORDER BY weight;
SELECT * FROM {profile_fields} WHERE fid = %d;
SELECT fid FROM {profile_fields} WHERE title = '%s' AND category = '%s';
SELECT fid FROM {profile_fields} WHERE name = '%s';
SELECT title FROM {profile_fields} WHERE fid = %d;
SELECT * FROM {profile_fields} ORDER BY category, weight;
SELECT DISTINCT(fid), type, title, page, visibility FROM {profile_fields} WHERE name = '%s';
SELECT name, title, type, weight, page FROM {profile_fields} WHERE fid != %d AND visibility = %d ORDER BY weight;
SELECT u.uid, u.access FROM {users} u INNER JOIN {profile_values} v ON u.uid = v.uid WHERE v.fid = %d AND $query AND u.access != 0 AND u.status != 0 ORDER BY u.access DESC;
SELECT name, title, type, weight, page FROM {profile_fields} WHERE visibility = %d ORDER BY category, weight;
SELECT uid, access FROM {users} WHERE uid > 0 AND status != 0 AND access != 0 ORDER BY access DESC;
SELECT f.name, f.type, v.value FROM {profile_fields} f INNER JOIN {profile_values} v ON f.fid = v.fid WHERE uid = %d;
SELECT fid, name, type FROM {profile_fields} WHERE register = 1 AND visibility != %d ORDER BY category, weight;
SELECT fid, name, type FROM {profile_fields} WHERE LOWER(category) = LOWER('%s') AND visibility != %d;
SELECT * FROM {profile_fields} WHERE visibility != %d ORDER BY category, weight;
SELECT * FROM {profile_fields} WHERE visibility != %d AND visibility != %d ORDER BY category, weight;
SELECT * FROM {profile_fields} WHERE visibility != %d AND register = 1 ORDER BY category, weight;
SELECT * FROM {profile_fields} WHERE register = 1 ORDER BY category, weight;
SELECT * FROM {profile_fields} WHERE LOWER(category) = LOWER('%s') ORDER BY weight;
SELECT * FROM {profile_fields} WHERE visibility != %d AND LOWER(category) = LOWER('%s') ORDER BY weight;
SELECT * FROM {profile_fields} WHERE visibility != %d AND register = 1 ORDER BY category, weight;
SELECT * FROM {profile_fields} WHERE register = 1 ORDER BY category, weight;
SELECT * FROM {profile_fields} WHERE LOWER(category) = LOWER('%s') ORDER BY weight;
SELECT * FROM {profile_fields} WHERE visibility != %d AND LOWER(category) = LOWER('%s') ORDER BY weight;
SELECT DISTINCT(category) FROM {profile_fields};
