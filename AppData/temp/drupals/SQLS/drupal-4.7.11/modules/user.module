SELECT uid FROM {authmap} WHERE authname = '%s;
-- SELECT * FROM {users} u WHERE ' . implode(' AND ;
SELECT r.rid, r.name FROM {role} r INNER JOIN {users_roles} ur ON ur.rid = r.rid WHERE ur.uid = %d;
SELECT data FROM {users} WHERE uid = %d;
SELECT DISTINCT(p.perm) FROM {role} r INNER JOIN {permission} p ON p.rid = r.rid WHERE r.rid IN (0,1);
SELECT * FROM {users} WHERE status = 1 AND name = LOWER('%s');
SELECT * FROM {users} WHERE status = 0 AND name = LOWER('%s');
SELECT * FROM {users} WHERE uid = 1;
SELECT * FROM {users} WHERE LOWER(name) LIKE LOWER('%%%s%%';
SELECT uid, name FROM {users} WHERE status != 0 AND access != 0 ORDER BY created DESC;
SELECT COUNT(sid) AS count FROM {sessions} WHERE timestamp >= %d AND uid = 0;
SELECT uid, name, access FROM {users} WHERE access >= %d AND uid != 0 ORDER BY access DESC;
SELECT authname, module FROM {authmap} WHERE authname = '%s';
SELECT uid FROM {users} WHERE uid != %d AND LOWER(name) = LOWER('%s');
SELECT uid FROM {users} WHERE uid != %d AND LOWER(mail) = LOWER('%s');
SELECT aid, type, status, mask FROM {access} WHERE aid = %d;
SELECT aid, type, status, mask FROM {access} WHERE aid = %d;
SELECT aid, type, status, mask FROM {access};
SELECT r.* FROM {role} r INNER JOIN {permission} p ON r.rid = p.rid WHERE p.perm LIKE '%%%s%%';
SELECT * FROM {role} ORDER BY name;
SELECT r.rid, p.perm FROM {role} r LEFT JOIN {permission} p ON r.rid = p.rid WHERE r.rid IN (0,1) ORDER BY name;
SELECT r.rid, p.perm FROM {role} r LEFT JOIN {permission} p ON r.rid = p.rid ORDER BY name;
SELECT rid, name FROM {role} r WHERE r.rid IN (0,1) ORDER BY name;
SELECT rid, name FROM {role} ORDER BY name;
SELECT * FROM {role};
SELECT * FROM {role} WHERE rid = %d;
SELECT COUNT(*) FROM {role} WHERE name = '%s' AND rid != %d;
SELECT COUNT(*) FROM {role} WHERE name = '%s';
SELECT u.uid, u.name, u.status, u.created, u.access FROM {users} u WHERE uid != 0;
SELECT name FROM {users} WHERE LOWER(name) LIKE LOWER('%s%%');
