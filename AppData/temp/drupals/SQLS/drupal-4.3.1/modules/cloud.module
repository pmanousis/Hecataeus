SELECT * FROM {site} WHERE checked = 0 OR checked + refresh < %d;
SELECT * FROM {site} WHERE sid = %d;
SELECT * FROM {site} ORDER BY name;
SELECT * FROM {site} WHERE changed > ". (time() - 604800) ." ORDER BY changed DESC;
