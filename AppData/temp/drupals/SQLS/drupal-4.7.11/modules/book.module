SELECT n.nid FROM node n WHERE n.nid = 0 AND n.type != 'book';
SELECT n.nid, n.title, b.parent FROM node n INNER JOIN book b ON n.vid = b.vid WHERE n.nid = 0;
SELECT * FROM book WHERE vid = 0;
SELECT n.nid, n.title, b.parent, b.weight FROM node n INNER JOIN book b ON n.vid = b.vid WHERE n.nid = 0;
SELECT n.nid, n.title, b.parent, b.weight FROM node n INNER JOIN book b ON n.vid = b.vid WHERE n.status = 1 AND b.parent = 0 ORDER BY b.weight DESC, n.title DESC;
SELECT n.nid, n.title, b.weight FROM node n INNER JOIN book b ON n.vid = b.vid WHERE b.parent = 0 AND n.status = 1 AND n.moderate = 0 AND (b.weight < 0 OR (b.weight = 0 AND n.title < '%s')) ORDER BY b.weight DESC, n.title DESC;
SELECT n.nid, n.title FROM node n INNER JOIN book b ON n.vid = b.vid WHERE n.nid = 0 AND n.status = 1 AND n.moderate = 0;
SELECT n.nid, n.title, b.weight FROM node n INNER JOIN book b ON n.vid = b.vid WHERE b.parent = 0 AND n.status = 1 AND n.moderate = 0 ORDER BY b.weight ASC, n.title ASC;
SELECT n.nid, n.title, b.weight FROM node n INNER JOIN book b ON n.vid = b.vid WHERE b.parent = 0 AND n.status = 1 AND n.moderate = 0 AND (b.weight > 0 OR (b.weight = 0 AND n.title > '%s')) ORDER BY b.weight ASC, n.title ASC;
SELECT * FROM book WHERE vid = 0;
SELECT n.nid, n.title, b.parent, b.weight FROM node n INNER JOIN book b ON n.vid = b.vid WHERE n.status = 1 ORDER BY b.weight, n.title;
SELECT n.nid, n.title, b.parent, b.weight FROM node n INNER JOIN book b ON n.vid = b.vid WHERE n.status = 1 AND n.moderate = 0 ORDER BY b.weight, n.title;
SELECT n.nid, n.title, b.weight FROM node n INNER JOIN book b ON n.vid = b.vid WHERE b.parent = 0 AND n.status = 1 AND n.moderate = 0 ORDER BY b.weight, n.title;
SELECT n.nid, n.title, b.parent FROM node n INNER JOIN book b ON n.vid = b.vid WHERE n.nid = 0;
SELECT n.nid, n.title, b.weight FROM node n INNER JOIN book b ON n.vid = b.vid WHERE n.status = 1 AND n.nid = 0 AND n.moderate = 0 ORDER BY b.weight, n.title;
SELECT n.nid, n.title, b.weight FROM node n INNER JOIN book b ON n.vid = b.vid WHERE n.status = 1 AND b.parent = 0 AND n.moderate = 0 ORDER BY b.weight, n.title;
SELECT n.nid, n.title, b.weight FROM node n INNER JOIN book b ON n.vid = b.vid WHERE b.parent = 0 ORDER BY b.weight, n.title;
SELECT n.nid, n.title, n.status, b.parent FROM node n INNER JOIN book b ON n.vid = b.vid;
SELECT n.nid, n.title, b.weight FROM node n INNER JOIN book b ON n.vid = b.vid WHERE b.parent = 0 ORDER BY b.weight, n.title;
